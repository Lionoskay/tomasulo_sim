# 上机实验 3：Tomasulo 动态调度模拟器 — 实验报告

| 项目 | 内容 |
|------|------|
| 姓名 |  |
| 学号 |  |
| 班级 |  |
| 日期 |  |

---

## 目录

1. [实验目的](#1-实验目的)
2. [实验平台](#2-实验平台)
3. [模拟器设计思想与特色](#3-模拟器设计思想与特色)
   - 3.1 整体架构与数据流
   - 3.2 数据通路层（datapath.py）
   - 3.3 汇编器（assembler.py）
   - 3.4 Tomasulo 核心引擎（tomasulo.py）
   - 3.5 Flask API 层（app.py）
   - 3.6 前端可视化
4. [测试代码组合](#4-测试代码组合)
5. [测试代码执行过程与分析](#5-测试代码执行过程与分析)
   - 5.1 场景一：无冲突基本流水
   - 5.2 场景二：RAW 数据冲突
   - 5.3 场景三：WAR 冲突 + RAW 混合
6. [性能统计对比](#6-性能统计对比)
7. [实验感悟](#7-实验感悟)



## 1. 实验目的

1. 深入理解 Tomasulo 动态调度算法的核心思想：寄存器重命名、保留站、CDB 公共数据总线；
2. 掌握 Tomasulo 算法三阶段流水线（流出/执行/写结果）的时序关系；
3. 理解 WAR（反相关）和 WAW（写后写）冲突如何通过寄存器重命名消除；
4. 理解 RAW（真数据依赖）冲突在动态调度下的表现，掌握 Load-Use 依赖和 CDB 广播延迟对性能的影响；
5. 对比 Tomasulo 动态调度与经典五段流水线在冲突处理上的差异。

## 2. 实验平台
本次项目采用与五级流水线模拟器相同的设计思想，为了实现便捷化的网页交互流程和直观的数据呈现方式，采用网页前端和python后端分离的方式运行，前后端之间通过Flask运用REST json api数据包进行通信。详细的介绍如下表所示：
| 项目 | 选择 |
|------|------|
| 后端语言 | Python 3 |
| Web 框架 | Flask |
| 前端 | 原生 HTML/CSS/JS（单页应用） |
| 体系结构 | MIPS 浮点指令子集（Tomasulo 动态调度） |
| 运行方式 | `python app.py` 本地启动，浏览器访问 `http://localhost:5050` |

**项目结构：**
本项目包含下面的几个文件，各个文件之间的关系如下所示：
```
tomasulo/
├── app.py                 # Flask 入口：API 路由 + 启动服务器
├── tomasulo.py            # Tomasulo 核心引擎：三阶段流水线 + CDB + 冲突处理
├── assembler.py           # 汇编器：MIPS 浮点指令文本 → 内部表示
├── datapath.py            # 数据通路：寄存器堆、内存、功能部件延迟配置
├── templates/
│   └── index.html         # 前端页面：保留站 + 寄存器状态 + 指令状态 + 统计面板
└── test_programs/
    ├── no_conflict.txt    # 场景1：无 WAR/WAW 冲突
    ├── raw_conflict.txt   # 场景2：RAW 冲突
    └── war_conflict.txt   # 场景3：WAR 冲突 + RAW 混合
```

本次试验设计的tomasulo算法模拟器**支持的指令集**包括 `l.d`, `s.d`, `add.d`, `sub.d`, `mul.d`, `div.d`，默认情况下预先设置了包括保留站数量之类的**硬件资源：**
- FP 加法保留站 x3（Add1, Add2, Add3）
- FP 乘法保留站 x2（Mult1, Mult2）
- Load Buffer x3（Load1, Load2, Load3）
- Store Buffer x3（Store1, Store2, Store3）
- 16 个双精度 FP 寄存器（仅偶数编号：f0, f2, ..., f30）
- 32 个 32 位整数寄存器（R0 硬连线为 0）

与教材自带的模拟器类似，本模拟器也支持手动调整每个指令的执行时间，为了方便程序调试，提前设置了**默认的功能部件延迟**，如下表所示：

| 指令 | 延迟（周期） |
|------|:---------:|
| l.d | 2 |
| s.d | 1 |
| add.d / sub.d | 2 |
| mul.d | 10 |
| div.d | 40 |



## 3. 模拟器设计思想

模拟器采用**前后端分离架构**，后端用 Python + Flask 实现完整的 Tomasulo 动态调度算法和 CDB 广播机制，前端用原生 HTML/CSS/JS 构建可视化仪表盘，两者通过 REST JSON API 通信。

### 3.1 整体架构与数据流

```
┌──────────────────────────────────────────────────────────────────────────┐
│                        数据流全景                                          │
│                                                                          │
│  用户输入 MIPS 汇编代码 (字符串)                                             │
│         │                                                                │
│         ▼                                                                │
│    assemble() → 解析为 Instruction 列表     ← 纯函数，正则匹配解析            │
│         │                                                                │
│         ▼                                                                │
│    TomasuloEngine.load_program()           ← 初始化硬件 + 指令状态表        │
│         │                                                                │
│         ▼  每个时钟周期 step() 按以下顺序执行                                 │
│   1. _promote_exec_done()  ← exec_done → write_ready 推一周期              │
│   2. _execute()            ← 各部件倒计数 + RAW 停顿检测                    │
│   3. _write_result()       ← 收集 write_ready 条目入队                     │
│   4. _flush_cdb()          ← CDB 广播：写寄存器 + 传播到等待站               │
│   5. _issue()              ← 取指令 → 寄存器重命名 → 分配到保留站/Buffer      │
│         │                                                                │
│         ▼                                                                │
│   to_snapshot() → 完整状态 JSON → HTTP Response → 前端渲染                 │
└──────────────────────────────────────────────────────────────────────────┘
```
整体的模拟器数据流如上所示：
1. 首先用户从网页端输入MIPS汇编指令代码，模拟器将其存储为字符串后进入assemble()
2. 在assemble()中将输入的字符串列表进行解析，将其按行解析为指令列表，并存储指令
3. 在TomasuloEngine.load_program()中将指令加载到每一个模拟硬件中，随后执行step()
4. 在step()方法中，模拟每个时钟周期内要按顺序完成的几个步骤：包括**写回**,**执行**,**流出**,**停顿检测**等。
5. 最后通过to_snapshot()统一收集所有的信息并完成JSON打包，发送给前端进行渲染，

### 关键设计一：ISSUE滞后
在step()方法中存在一个**关键的设计：将ISSUE指令流出放在一个周期的最后阶段**，如下面指令所示，step()方法的_issue()方法放置在一个周期内的最后一步：

`tomasulo.py` 的 `step()` 方法（第 320-349 行）展示了核心的执行顺序：

```python
def step(self):
    self.cycle += 1
    self.done = False

    # 0. 将上一周期执行完成的条目标记为可写结果
    self._promote_exec_done()

    # 1. 执行阶段
    self._execute()

    # 2. 写结果阶段（入队）
    self._write_result()

    # 3. CDB 广播（与写结果同一周期）
    self._flush_cdb()

    # 4. 流出阶段
    self._issue()

    # 检查是否所有指令已完成
    self._check_done()

    # 保存历史快照
    self._save_history()
```

选择**Issue 放在最后**而非第一位的设计考量：

Issue 放在末尾意味着刚流出的指令到下一周期才能开始执行。这与 Tomasulo 的经典实现一致——流出阶段不消耗执行资源，仅在下一周期开始后参与倒计数。同时，Execute → Write Result → CDB → Issue 的顺序**保证了同一周期内写回的值可以被本周期流出的指令捕获到寄存器状态表中**。

### 关键设计二：写回前延迟

为了模拟硬件中**执行完成到写回**之间存在的一个周期的流水线寄存器延迟。本项目通过两组标志位`exec_done`和`write_ready`实现这一时序：

```python
def _promote_exec_done(self):
    """将上一周期执行完成的条目（exec_done）提升为可写结果（write_ready）。"""
    for rs in self.add_rs + self.mul_rs:
        if rs.busy and rs.exec_done:
            rs.exec_done = False
            rs.write_ready = True
    # Load/Store Buffer 同理
```
在上述代码所示的_promote_exec_done()方法中，在执行完成的下一个周期内，如果检测到目标功能部件busy且执行完成的标志位`exec_done`被置位，则将`write_ready`标志位置位，允许写回。

为了更直观的展示本模拟器的时序流程，将**完整时序链**列出：
```
周期 N:   execute 结束 → exec_done = True
周期 N+1: _promote_exec_done → write_ready = True
          _write_result → 入队 _pending_cdb
          _flush_cdb → CDB 广播写寄存器
周期 N+2: 依赖指令的 qj/qk 在 _flush_cdb 时被清除
          同一周期 _execute 可以开始执行
```
- 周期N时：执行结束，将`exec_done`标志位置为“1”
- 周期N+1时：运行`_promote_exec_done`方法，将`write_ready`标志位置为“1”，允许写回，随后通过CDB广播写回寄存器
- 周期N+2时，功能部件获取到对应数据后可以执行。

这保证了：**执行完成 → 等 1 周期 → 写结果(同CDB) → 等 1 周期 → 依赖指令开始执行**，精确模拟了 Tomasulo 的 CDB 广播延迟。

### 3.2 数据通路层（datapath.py）

`datapath.py` 实现了 5 个独立的硬件组件类，每个对应 Tomasulo 数据通路中的一个真实部件：

| 类名 | 行号 | 对应硬件 | 核心接口 | 关键实现细节 |
|------|------|----------|---------|-------------|
| `FPRegisterFile` | 14-27 | FP 寄存器堆（16×64位双精度） | `read(name)`, `write(name, value)` | 仅支持偶数编号寄存器（f0, f2, ..., f30），与 MIPS FP 指令格式一致 |
| `IntRegisterFile` | 35-51 | 整数寄存器堆（32×32位） | `read(name)`, `write(name, value)` | R0 硬连线为 0：`write()` 中 `name == "0"` 直接 return，禁止写入 |
| `DataMemory` | 57-69 | 数据存储器（字节寻址） | `read_double(address)`, `write_double(address, value)` | 按字节地址寻址，支持双字存取；未写入的地址返回 0.0 |
| `InstructionMemory` | 74-83 | 指令存储器 | `load(addr)`, `clear()` | 按地址索引的指令字典，`clear()` 用于程序重置时清空 |
| `LatencyConfig` | 88-127 | 功能部件延迟配置 | `get(op)`, `from_dict(d)` | 映射指令到对应功能部件延迟，支持前端自定义延迟参数 |

本项目模拟的**FP 寄存器堆设计（第 14-27 行）**代码如下：

```python
FP_REGISTERS = [f"f{i}" for i in range(0, 32, 2)]

@dataclass
class FPRegisterFile:
    regs: dict[str, float] = field(default_factory=dict)

    def __post_init__(self):
        for name in FP_REGISTERS:
            self.regs.setdefault(name, 0.0)
```

MIPS 架构的双精度浮点寄存器只有偶数编号（f0/f2/f4/.../f30），共 16 个。`FP_REGISTERS` 列表通过 `range(0, 32, 2)` 生成正确的编号序列，每个寄存器初始值为 0.0。

**功能部件延迟配置（第 88-127 行）**代码如下：

```python
@dataclass
class LatencyConfig:
    load: int = 2
    store: int = 1
    add: int = 2
    sub: int = 2
    mul: int = 10
    div: int = 40

    def get(self, op: str) -> int:
        mapping = {
            "l.d": self.load, "s.d": self.store,
            "add.d": self.add, "sub.d": self.sub,
            "mul.d": self.mul, "div.d": self.div,
        }
        return mapping.get(op, 1)
```

`LatencyConfig` 使用 `@dataclass` 定义，每个字段对应一个功能部件的执行延迟。首先设置默认的功能部件延迟，如果要自行设置延迟，可以通过`get(op)` 方法将指令操作码映射到对应延迟值，未匹配的操作码默认返回 1。

### 3.3 汇编器（assembler.py）

`assembler.py` 的功能是将用户输入的 MIPS 浮点汇编文本解析为模拟器内部使用的 `Instruction` 数据类。

**核心数据结构 — Instruction（第 10-31 行）：**

```python
@dataclass
class Instruction:
    addr: int          # 指令地址（从 0 开始，每指令 4 字节）
    op: str            # 操作码: l.d / s.d / add.d / sub.d / mul.d / div.d
    rd: Optional[str]  # 目标寄存器
    rs: Optional[str]  # 源寄存器 1（整数基址 或 FP 源操作数）
    rt: Optional[str]  # 源寄存器 2（FP 数据寄存器 或 FP 第二源操作数）
    offset: int = 0    # 立即数偏移（仅 load/store）
    text: str = ""     # 原始汇编文本
```
在`assembler.py`中将指令设置为一个类，包含MIPS32指令的所有信息，方便对指令进行赋值等操作。

**指令分类属性（第 22-30 行）：**

```python
@property
def is_load(self) -> bool:
    return self.op == "l.d"

@property
def is_store(self) -> bool:
    return self.op == "s.d"

@property
def is_fp_arith(self) -> bool:
    return self.op in ("add.d", "sub.d", "mul.d", "div.d")
```

三个 `@property` 装饰器将指令分为 Load、Store、FP 算术三类，用于 Tomasulo 引擎根据指令类型将指令分配到不同的功能部件（Load Buffer / Store Buffer / 保留站）。

**寄存器校验集合（第 43-45 行）：**

```python
VALID_FP_REGS = {f"$f{i}" for i in range(0, 32, 2)}
VALID_INT_REGS = {f"R{i}" for i in range(32)}
```

FP 寄存器仅允许偶数编号（$f0, $f2, ..., $f30），整数寄存器支持 R0-R31。

**正则解析（第 48-53 行）：**

```python
RE_LOAD_STORE = re.compile(
    r"^(l\.d|s\.d)\s+(\$f\d+)\s*,\s*(-?\d+)\s*\(\s*([rR]\d+)\s*\)\s*$"
)
RE_FP_ARITH = re.compile(
    r"^(add\.d|sub\.d|mul\.d|div\.d)\s+(\$f\d+)\s*,\s*(\$f\d+)\s*,\s*(\$f\d+)\s*$"
)
```

两条正则分别匹配 Load/Store 和 FP 算术指令格式：

- **Load/Store**：`l.d $f0, 0(R1)` → 操作码 + FP 寄存器 + 偏移量 + 基址寄存器
- **FP 算术**：`add.d $f4, $f0, $f2` → 操作码 + 目标寄存器 + 源寄存器1 + 源寄存器2

**parse_line() 函数（第 56-85 行）** 按顺序尝试两种正则匹配，若都不匹配则抛出 `ParseError` 并附带行号。注释行（以 `#` 开头）和空行被过滤掉返回 `None`。

**assemble() 函数（第 88-101 行）** 循环调用 `parse_line()`，每条有效指令地址递增 4 字节，最终返回指令列表。

### 3.4 Tomasulo 核心引擎（tomasulo.py）

`tomasulo.py` 是模拟器的核心，包含 6 个数据结构和 5 个子系统。以下逐一分析。

#### 3.4.1 核心数据结构

##### (1) ReservationEntry — 保留站条目（第 17-60 行）

```python
@dataclass
class ReservationEntry:
    name: str
    busy: bool = False
    op: Optional[str] = None
    vj: Optional[float] = None
    vk: Optional[float] = None
    qj: Optional[str] = None          # 等待源操作数 1 的生产者 tag
    qk: Optional[str] = None          # 等待源操作数 2 的生产者 tag
    remaining: int = 0                # 剩余执行周期
    dest: Optional[str] = None        # 目标寄存器名
    inst_addr: Optional[int] = None   # 对应指令地址
    vj_label: Optional[str] = None    # 源操作数 1 的符号标签（如 M1）
    vk_label: Optional[str] = None    # 源操作数 2 的符号标签
    exec_done: bool = False           # 执行已完成，等待进入写结果
    write_ready: bool = False         # 已可写结果（exec_done 推一周期后）
```

**各字段含义：**

- `qj/qk` 为 `None` 表示操作数已就绪，非 `None` 表示正在等待某个生产者（保留站名或 Load Buffer 名）通过 CDB 广播其值
- `vj/vk` 存储操作数的数值（浮点数），当其对应的 `qj/qk` 为 `None` 时有效
- `vj_label/vk_label` 存储操作数的**符号标签**（如 `R[f0]` 表示初始寄存器值，`M1` 表示某条指令通过 CDB 写入的值）
- `exec_done/write_ready` 实现执行完成到写结果的**一周期延迟**：第 N 周期执行完成设置 `exec_done=True`，第 N+1 周期 `_promote_exec_done()` 转为 `write_ready=True`，同周期写结果

##### (2) LoadStoreEntry — Load/Store Buffer 条目（第 66-136 行）

```python
@dataclass
class LoadStoreEntry:
    name: str
    busy: bool = False
    op: Optional[str] = None       # "l.d" / "s.d"
    address: Optional[int] = None  # 有效地址（数值）
    value: Optional[float] = None  # Load 的加载值 / Store 的待存值
    remaining: int = 0
    dest: Optional[str] = None     # Load 的目标 FP 寄存器
    # 内部字段
    qj: Optional[str] = None       # 等待基址寄存器（整数寄存器，总是就绪）
    qk: Optional[str] = None       # Store 等待数据寄存器（FP）
    vj: Optional[int] = None       # 基址寄存器就绪值
    vk: Optional[float] = None     # Store 数据就绪值
    inst_addr: Optional[int] = None
    address_ready: bool = False    # Store：有效地址已计算
    data_ready: bool = False       # Store：数据已就绪
    exec_done: bool = False
    write_ready: bool = False
    # 符号显示用
    base_reg: Optional[str] = None # 基址寄存器名（如 "R1"）
    store_src: Optional[str] = None# Store 的源 FP 寄存器
    offset: int = 0
```

**Load Buffer 与 Store Buffer 的差异：**

| 字段 | Load Buffer | Store Buffer |
|------|:-----------:|:------------:|
| `dest` | 目标 FP 寄存器 | `None`（不写寄存器） |
| `qk` | 不使用 | 等待存储数据的生产者 tag |
| `vk` | 不使用 | 存储数据的数值 |
| `data_ready` | 不适用 | 数据是否已就绪 |
| `address_ready` | 总是 True | 地址是否已计算 |

**to_dict() 方法（第 90-115 行）** 构造前端展示的地址和值字符串：

- Load 地址显示为 `R[Rn]+offset`（如 `R[1]+0`）
- Load 值在执行完成后显示为 `M[R[Rn]+offset]`（如 `M[R[1]+0]`）
- Store 值在数据就绪时显示数值，否则显示源寄存器名

Store Buffer 是唯一**不写寄存器**的功能部件，它仅将数据写入内存。因此 Store 指令在 Issue 时不更新寄存器状态表。

##### (3) RegisterStatusEntry — 寄存器状态条目（第 140-152 行）

```python
@dataclass
class RegisterStatusEntry:
    qi: Optional[str] = None   # 生产者 tag，None 表示值已就绪
    value: Optional[float] = 0.0
    value_label: Optional[str] = None   # 值的符号标签（如 M1, R[f0]）
    computation: Optional[str] = None   # 计算表达式（如 "M1 + M2"）
```

**`qi`（寄存器重命名的核心）：**

- `qi` 非 `None` 时表示该寄存器正在被某个功能部件写入。后续读取该寄存器的指令将捕获此 `qi` tag 到自己的 `qj/qk` 中，而非读取数值
- `qi` 为 `None` 时表示寄存器的值已就绪，可直接读取 `value` 和 `value_label`

**to_dict() 方法（第 147-152 行）：**

```python
def to_dict(self) -> dict:
    return {
        "qi": self.qi,
        "value": self.value_label if (self.value_label and self.qi is None)
                 else (self.value if self.qi is None else None),
        "computation": self.computation if self.qi is None else None,
    }
```

只有当 `qi is None`（值已就绪）时才向前端显示值和计算表达式。`value` 字段优先使用符号标签。

**值标签命名规则（Mn 序号系统）：**

模拟器维护一个全局的 `_value_counter` 和 `_value_labels` 字典（第 241-242 行）：

```python
self._value_counter: int = 0
self._value_labels: dict[str, str] = {}  # tag → "Mn"
```

每当 CDB 广播时，计数器递增并分配 Mn 序号：

```python
self._value_counter += 1
self._value_labels[tag] = f"M{self._value_counter}"
```

Mn 序号**按 CDB 广播顺序（即写回顺序）递增**，而非指令流出顺序。例如，如果 Load1（第 1 条指令）执行需要 2 周期而 Add1（第 3 条指令）执行需要 2 周期，且流出的相对时间使得 Add1 先执行完成，则 Add1 的结果为 M1，Load1 的结果为 M2。

**计算表达式（computation）的生成：**

- **Load 指令**：`computation = "MEM[R{n}+{offset}]"`（第 652-654 行 of `_cdb_broadcast_load`）
- **FP 算术指令**：`computation = "{vj_label} {op_symbol} {vk_label}"`（第 625-628 行 of `_cdb_broadcast_rs`），例如 `"M1 + M2"`, `"R[f0] * M3"`

##### (4) InstructionStatusEntry — 指令状态条目（第 157-176 行）

```python
@dataclass
class InstructionStatusEntry:
    addr: int
    text: str
    issue: Optional[int] = None          # 流出周期
    execute_start: Optional[int] = None  # 开始执行周期
    execute_end: Optional[int] = None    # 执行完成周期
    write_result: Optional[int] = None   # 写回周期
    done: bool = False
```

每条指令在程序加载时创建对应的 `InstructionStatusEntry`，随着流水线推进逐步填充各字段。`done` 标志在 `write_result` 被设置后标记为 `True`。

##### (5) Event — 事件日志条目（第 181-185 行）

```python
@dataclass
class Event:
    cycle: int
    type: str  # "issue" / "execute" / "cdb" / "stall"
    message: str
```

事件日志记录流水线的关键动作，用于在前端事件面板中展示实时执行轨迹。

#### 3.4.2 流出阶段（Issue）— 第 352-503 行

流出阶段从指令队列中取一条指令（由 PC 指向），根据指令类型将指令分配到对应功能部件。

**流出条件检查：**

每种指令类型对应特定的功能部件资源，若该类型的所有功能部件均已被占用（`busy == True`），则发生**结构冲突**，指令在当前周期不流出：

```python
buf = self._find_free_load_buffer()
if buf is None:
    self.stats["structural_stalls"] += 1
    self._log("stall", f"Load Buffer 全满，l.d 流出阻塞")
    return
```

**Load 指令流出（_issue_load，第 372-403 行）：**

```python
def _issue_load(self, inst: Instruction):
    buf = self._find_free_load_buffer()
    ...
    buf.busy = True
    buf.op = "l.d"
    buf.dest = self._fp_name(inst.rd)
    buf.remaining = self.latency.get("l.d")
    buf.base_reg = inst.rs
    buf.offset = inst.offset

    # 基址寄存器总是就绪（整数寄存器不受 Tomasulo 调度）
    int_val = self.int_regs.read(rs)
    buf.vj = int_val
    buf.address = int_val + inst.offset
    buf.address_ready = True

    # 更新目标寄存器状态：标记为 Load Buffer 正在写入
    self.register_status[inst.rd].qi = buf.name
```

- Load 的基址寄存器是整数寄存器，在 Tomasulo 调度中总是就绪，不需要等待
- 流出后立即将目标 FP 寄存器的 `qi` 设为该 Load Buffer 的名称（如 "Load1"），使后续依赖该寄存器的指令捕获此 tag

**Store 指令流出（_issue_store，第 406-446 行）：**

```python
def _issue_store(self, inst: Instruction):
    ...
    # 基址寄存器（整数寄存器，总是就绪）
    buf.vj = int_val
    buf.address = int_val + inst.offset
    buf.address_ready = True

    # 数据寄存器（FP 寄存器，需要通过 Tomasulo 调度等待）
    ft = self._fp_name(inst.rt)
    st = self.register_status[ft]
    if st.qi is not None:
        buf.qk = st.qi       # 数据未就绪：捕获生产者 tag
        buf.data_ready = False
    else:
        buf.qk = None
        buf.vk = st.value    # 数据已就绪：直接获取值
        buf.data_ready = True
```

Store 不更新任何寄存器状态表（不写寄存器）。其数据源寄存器（`rt`）通过 Tomasulo 的常规 CDB 机制等待。

**FP 算术指令流出（_issue_fp_arith，第 448-503 行）：**

这是寄存器重命名的核心代码：

```python
def _issue_fp_arith(self, inst: Instruction):
    # 选择对应保留站类型
    if inst.op == "mul.d" or inst.op == "div.d":
        rs_list = self.mul_rs
    else:
        rs_list = self.add_rs

    # 源操作数 1：捕获寄存器状态
    st_rs = self.register_status[inst.rs]
    if st_rs.qi is not None:
        free_rs.qj = st_rs.qi        # 等待生产者
        free_rs.vj = None
    else:
        free_rs.qj = None
        free_rs.vj = st_rs.value     # 直接取值
        free_rs.vj_label = st_rs.value_label  # 捕获符号标签

    # 源操作数 2：同上逻辑
    st_rt = self.register_status[inst.rt]
    ...

    # 覆盖目标寄存器状态（寄存器重命名）
    self.register_status[inst.rd].qi = free_rs.name
    self.register_status[inst.rd].value = None
    self.register_status[inst.rd].value_label = None
```

**寄存器重命名的核心机制：**
1. 读出源寄存器 `rs` 和 `rt` 的状态：若 `qi` 非 `None`（有前序指令正在写该寄存器），则将 `qi` 复制到保留站的 `qj/qk` 中；若 `qi` 为 `None`（值已就绪），则直接读取数值和符号标签
2. 覆盖目标寄存器 `rd` 的状态：将 `qi` 设为当前保留站名，`value` 和 `value_label` 清空。这意味着后续指令读取 `rd` 时将捕获当前保留站的 tag

**WAR 冲突的消除：** 若流出时 `rs` 或 `rt` 的值已经就绪（`qi is None`），即使后续有指令要覆盖这些寄存器，当前指令已经从寄存器状态表中"快照"了值到自己的 `vj/vk` 中。这种"早期值捕获"正是 Tomasulo 消除 WAR 冲突的关键。

**WAW 冲突的消除：** 目标寄存器 `rd` 的 `qi` 被覆盖为新指令的保留站名（而非旧生产者名）。当旧生产者的结果通过 CDB 广播时，会检查 `register_status[dest].qi == tag` 条件（第 620 行），若不匹配则不写寄存器，从而避免了旧值覆盖新值的 WAW 冲突。

**符号标签的传播链：**

```
流出时: 源寄存器 value_label = "R[f0]"  → 保留站 vj_label = "R[f0]"
CDB时:  保留站 tag = "Add1"  →  _value_labels["Add1"] = "M1"
         寄存器状态 value_label = "M1", computation = "R[f0] + R[f2]"
后续流出: 寄存器状态 value_label = "M1"  → 保留站 vj_label = "M1"
后续CDB:  computation = "M1 + R[f6]"
```

#### 3.4.3 执行阶段（Execute）— 第 507-571 行

执行阶段遍历所有功能部件，对正在忙碌的条目进行倒计数。

```python
def _execute(self):
    # 保留站执行
    for rs in self.add_rs + self.mul_rs:
        if not rs.busy:
            continue
        if rs.write_ready:       # 等待写结果的条目跳过
            continue
        if rs.qj is not None or rs.qk is not None:
            self.stats["raw_stalls"] += 1  # RAW 停顿
            continue
        if rs.remaining == self.latency.get(rs.op):
            # 首次执行：记录 execute_start
            self._set_inst_status(rs.inst_addr, execute_start=self.cycle)
        if rs.remaining > 0:
            rs.remaining -= 1
        if rs.remaining == 0:
            self._set_inst_status(rs.inst_addr, execute_end=self.cycle)
            rs.exec_done = True
```

**RAW 停顿检测（第 515-518 行）：**

```python
if rs.qj is not None or rs.qk is not None:
    self.stats["raw_stalls"] += 1
    continue
```

当保留站的任一源操作数未就绪（`qj` 或 `qk` 非 `None`）时，该保留站本周期无法开始或继续执行。每次跳过执行时 `raw_stalls` 计数器 +1。这是 Tomasulo 下唯一的停顿类型——结构冲突在流出阶段处理，WAR/WAW 已被寄存器重命名消除。

**Store Buffer 执行的特殊处理（第 550-571 行）：**

```python
for buf in self.store_buffers:
    ...
    if not buf.data_ready:       # 数据未就绪 → RAW
        self.stats["raw_stalls"] += 1
        continue
    if self._has_earlier_store(buf):  # 更早的 Store 未完成 → 保持顺序
        continue
```

Store 必须按**程序顺序**执行（保证存储一致性）。`_has_earlier_store()` 方法（第 741-749 行）检查是否有更早（指令地址更小）的 Store Buffer 仍在忙碌。

#### 3.4.4 写结果阶段（Write Result）— 第 575-593 行

```python
def _write_result(self):
    for rs in self.add_rs + self.mul_rs:
        if rs.busy and rs.write_ready and rs.op is not None:
            self._set_inst_status(rs.inst_addr, write_result=self.cycle)
            self._pending_cdb.append(("rs", rs))

    for buf in self.load_buffers:
        if buf.busy and buf.write_ready and buf.op is not None:
            self._set_inst_status(buf.inst_addr, write_result=self.cycle)
            self._pending_cdb.append(("load", buf))
```

该阶段收集所有 `write_ready` 的条目，将它们加入 `_pending_cdb` 队列。`write_result` 周期被记录为指令的 `write_result` 字段。

#### 3.4.5 CDB 广播（Flush CDB）— 第 597-686 行

CDB 广播是 Tomasulo 算法的核心——将计算结果同时写入寄存器和传播到所有等待该结果的保留站/Buffer。

**_cdb_broadcast_rs() — 保留站结果广播（第 609-632 行）：**

```python
def _cdb_broadcast_rs(self, rs: ReservationEntry):
    tag = rs.name
    self._value_counter += 1
    self._value_labels[tag] = f"M{self._value_counter}"
    result = self._compute_result(rs)

    self._broadcast(tag, result)  # 传播到等待站

    # 写目标寄存器（检查 WAW）
    dest = self._fp_name(rs.dest) if rs.dest else None
    if dest and self.register_status[dest].qi == tag:
        self.register_status[dest].qi = None
        self.register_status[dest].value = result
        self.register_status[dest].value_label = self._value_labels.get(tag, tag)
        op_symbol = {"add.d": "+", "sub.d": "-", "mul.d": "*", "div.d": "/"}.get(rs.op, "?")
        vj_str = rs.vj_label or str(rs.vj or 0)
        vk_str = rs.vk_label or str(rs.vk or 0)
        self.register_status[dest].computation = f"{vj_str} {op_symbol} {vk_str}"
```

**关键步骤：**
1. **分配 Mn 序号**：`_value_counter += 1`，tag → Mn 的映射存入 `_value_labels`
2. **计算结果**：`_compute_result()` 根据操作码和 `vj/vk` 计算浮点结果
3. **全局广播**：`_broadcast(tag, result)` 将结果传播到所有等待该 tag 的保留站和 Store Buffer
4. **写寄存器（WAW 检查）**：只有当 `register_status[dest].qi == tag` 时才写入。若目标寄存器的 `qi` 已被后续指令覆盖（即 WAW 发生），本次写回被静默丢弃
5. **生成计算表达式**：将操作码符号和操作数标签拼接为可读表达式（如 `"M1 + M2"`）

**_broadcast() — 全局广播传播（第 659-680 行）：**

```python
def _broadcast(self, tag: str, value: float):
    value_label = self._value_labels.get(tag)
    # 遍历所有保留站
    for rs in self.add_rs + self.mul_rs:
        if rs.busy and rs.qj == tag:
            rs.qj = None
            rs.vj = value
            if value_label:
                rs.vj_label = value_label
        if rs.busy and rs.qk == tag:
            rs.qk = None
            rs.vk = value
            if value_label:
                rs.vk_label = value_label

    # 遍历 Store Buffer（数据寄存器）
    for buf in self.store_buffers:
        if buf.busy and buf.qk == tag:
            buf.qk = None
            buf.vk = value
            buf.data_ready = True
```

广播将 tag + value + value_label 推送到所有匹配的等待站点。`qj/qk` 被清除后，下一周期的执行阶段即可开始执行。

**_cdb_broadcast_load() — Load 结果广播（第 634-657 行）：**

与保留站广播类似的流程，但结果从内存读取（`self.data_mem.read_double(buf.address)`），且计算表达式标记为 `"MEM[R{n}+{offset}]"`。

**_complete_store() — Store 完成（第 682-687 行）：**

```python
def _complete_store(self, buf: LoadStoreEntry):
    self.data_mem.write_double(buf.address, buf.vk)
    buf.clear()
```

Store 在写结果阶段将数据写入内存，然后清空 Buffer。

#### 3.4.6 性能统计（第 248-252 行）

```python
self.stats = {
    "raw_stalls": 0,         # RAW 停顿（保留站等待操作数）
    "war_stalls": 0,         # WAR 停顿（Tomasulo 下恒为 0）
    "structural_stalls": 0,  # 结构停顿（功能部件全忙）
}
```

**CPI 计算（第 844 行）：**

```python
"cpi": round(total_cycles / completed, 2) if completed > 0 else 0
```

CPI = 总周期数 / 完成指令数。理想的 Tomasulo 流水线中 CPI 趋近于 1.0，RAW 冲突和结构冲突会导致 CPI 升高。

**统计汇总（第 838-845 行）：**

| 统计项 | 含义 | 发生条件 |
|--------|------|---------|
| `raw_stalls` | RAW（真依赖）停顿 | 保留站/Buffer 的 `qj` 或 `qk` 非 `None`，操作数未就绪 |
| `war_stalls` | WAR（反依赖）停顿 | Tomasulo 下恒为 0（寄存器重命名消除） |
| `structural_stalls` | 结构停顿 | 对应功能部件全满，新指令无法流出 |
| `cpi` | 每指令周期数 | `total_cycles / completed_instructions` |

#### 3.4.7 历史快照与状态导出（第 784-846 行）

`to_snapshot()` 方法将整个模拟器状态序列化为 JSON 字典：

```
┌───────────────────────────────────────────────────────────┐
│  SimulationSnapshot JSON 结构                              │
├───────────────────────────────────────────────────────────┤
│  ├─ cycle, pc, running, done                              │
│  ├─ registers: { integer: { R1: 100, ... } }              │
│  ├─ register_status: { f0~f30 → { qi, value, computation }}│
│  ├─ reservation_stations: { fp_add: [...], fp_mul: [...] }│
│  ├─ loadstore_buffers: [{ name, op, address, value, ... }]│
│  ├─ instruction_status: [{ addr, text, issue, exec, ... }]│
│  ├─ instructions: [{ addr, text }]                        │
│  ├─ data_memory: { addr → value }                         │
│  ├─ events: [...string...]                                │
│  └─ stats: { total_cycles, completed, raw_stalls, cpi }   │
└───────────────────────────────────────────────────────────┘
```

`get_history(cycle)` 方法（第 790-793 行）支持回溯到任意周期的完整状态快照，用于前端滑动查看历史。

### 3.5 Flask API 层（app.py）

`app.py` 将 `TomasuloEngine` 封装为 REST API：

| 方法 | 路径 | 功能 | 内部调用 |
|------|------|------|---------|
| GET | `/` | 返回前端页面 | `render_template("index.html")` |
| GET | `/api/state` | 获取当前状态 | `engine.to_snapshot()` |
| POST | `/api/step` | 单步执行 | `engine.step()`（可指定 cycles 参数） |
| POST | `/api/run` | 后台连续执行 | 启动 daemon 线程循环调用 `engine.step()` |
| POST | `/api/pause` | 暂停连续执行 | 设置 `_run_stop_flag` |
| POST | `/api/reset` | 重置模拟器 | `engine.reset()` |
| POST | `/api/load` | 载入汇编代码 | `engine.load_program(code, latencies)` |
| POST | `/api/history` | 获取历史快照 | `engine.get_history(cycle)` |

**连续执行的后台线程（第 68-74 行）：**

```python
def _run_loop(self):
    while engine.running and not engine.done:
        if _run_stop_flag.is_set():
            break
        engine.step()
        time.sleep(0.01)  # 10ms 延迟避免 CPU 空转
```

使用 Python `threading.Thread` daemon 线程在后台连续调度 `step()`，通过 `threading.Event` 实现暂停/停止控制。10ms 的 sleep 使得前端有足够时间在每周期之间通过轮询 `/api/state` 获取最新状态。

### 3.6 前端可视化

前端采用原生 HTML/CSS/JS 构建单页应用仪表盘，包含以下面板：

- **保留站面板**：展示 FP Add x3、FP Mul x2 保留站的 busy/op/vj/vk/qj/qk/remaining/dest 字段
- **Load/Store Buffer 面板**：展示 Load x3 + Store x3 的地址、值、执行状态
- **寄存器状态表**：展示 16 个 FP 寄存器的 qi/value/computation，以符号标签显示
- **指令状态表**：展示每条指令的 issue/exec_start/exec_end/write_result 周期
- **事件日志面板**：展示最近 20 条流水线事件
- **性能统计面板**：总周期数、完成指令数、CPI、RAW 停顿、结构停顿

### 3.7 主要特色总结

1. **完整的 Tomasulo 三阶段流水线**：Issue（流出 + 寄存器重命名）→ Execute（多周期执行 + RAW 检测）→ Write Result + CDB（广播写回 + WAW 检查）
2. **寄存器重命名消除 WAR/WAW**：通过保留站 tag 机制和 qi 字段实现动态寄存器重命名，WAR 和 WAW 冲突完全消除
3. **CDB 广播延迟精确建模**：exec_done/write_ready 两阶段晋升确保执行完成到写结果再到依赖指令执行各间隔 1 周期
4. **符号值跟踪系统**：Mn 序号按 CDB 广播顺序分配，支持计算表达式的推导和展示
5. **多种冲突场景验证**：无冲突、RAW 冲突、WAR+RAW 混合三个测试场景
6. **性能统计**：RAW 停顿、结构停顿、CPI 完整统计
7. **历史回溯**：支持滑动查看任意历史周期的完整快照

---

## 4. 测试代码组合

根据实验要求，设计以下 3 种场景的测试代码：

### 场景 1：无冲突基本流水（`no_conflict.txt`）

```asm
# 场景1：基本流水（无 WAR/WAW 冲突）
# 仅存在 RAW（真数据依赖），展示 Tomasulo 基本流水调度
l.d   $f0, 0(R1)
l.d   $f2, 8(R1)
add.d $f4, $f0, $f2
l.d   $f6, 16(R1)
add.d $f8, $f6, $f0
```

**特点**：
- 5 条指令，无 WAR/WAW 冲突
- 存在 RAW 依赖链：l.d→add.d（f0/f2）和 l.d→add.d（f6/f0）
- 所有依赖通过 CDB 广播正常解除

### 场景 2：RAW 数据冲突（`raw_conflict.txt`）

```asm
# 场景2：RAW 冲突
# mul.d 需等 l.d 完成（Load-Use 依赖）
# add.d 需等 mul.d 完成写 $f4（RAW）
# 验证保留站 Qj/Qk 正确标记，CDB 广播解除等待
l.d   $f0, 0(R1)
l.d   $f2, 8(R1)
mul.d $f4, $f0, $f2
add.d $f6, $f4, $f2
s.d   $f6, 16(R1)
```

**特点**：
- `mul.d` 依赖 l.d 的两个 load 结果（f0, f2），需要等待 CDB 广播
- `add.d` 依赖 `mul.d` 的 f4 结果（RAW），需要等待 mul.d 的 CDB 广播
- `s.d` 依赖 `add.d` 的 f6 结果
- 可以观察到保留站 qj/qk 的标记、清除和 RAW 停顿计数

### 场景 3：WAR 冲突 + RAW 混合（`war_conflict.txt`）

```asm
# 场景3：WAR 冲突 + RAW 混合
# add.d 读 $f2 旧值 → 流出时 Vj/Vk 直接取到值
# mul.d 覆盖 $f2 寄存器状态 → WAR 通过寄存器重命名解决
# 第二条 add.d 等待新 $f2 → RAW 正常处理
l.d   $f0, 0(R1)
l.d   $f2, 8(R1)
l.d   $f4, 16(R1)
add.d $f8, $f0, $f2
mul.d $f2, $f0, $f4
add.d $f10, $f2, $f8
```

**特点**：
- 第一条 `add.d` 读 `$f2` 的旧值（来自 `l.d $f2`），流出时直接拿到值
- `mul.d` 覆盖 `$f2` 的寄存器状态（WAR）：`register_status[f2].qi = "Mult1"`
- 由于第一条 `add.d` 已在流出时将 vj/vk 捕获，`mul.d` 覆盖 `$f2` 不会影响它 → **WAR 由寄存器重命名消除**
- 第二条 `add.d` 读 `$f2` 时捕获到 `Mult1` 的 qi → RAW 等待 mul.d 完成

---

## 5. 测试代码执行过程与分析

### 5.1 场景一：无冲突基本流水

#### 5.1.1 执行过程

> **[此处粘贴模拟器的网页截图，展示执行完成后的状态]**
>
> - 截图至少包含：保留站、Load/Store Buffer、寄存器状态表、指令状态表、事件日志、性能统计

**流水线时空分析（参考）：**



（Is = Issue, L = Load 执行周期, A = Add 执行周期, WB = Write Result+CDB, -- = RAW等待）

> **说明**：请根据实际运行结果填写上述时空图。

**执行分析：**







---

### 5.2 场景二：RAW 数据冲突

#### 5.2.1 执行过程

> **[此处粘贴模拟器的网页截图，展示执行完成后的状态]**
>
> - 截图至少包含：保留站、Load/Store Buffer、寄存器状态表、指令状态表、事件日志、性能统计

**流水线时空分析（参考）：**



**RAW 停顿的精确统计：**


### 5.3 场景三：WAR 冲突 + RAW 混合

#### 5.3.1 执行过程

> **[此处粘贴模拟器的网页截图，展示执行完成后的状态]**
>
> - 截图至少包含：保留站、Load/Store Buffer、寄存器状态表、指令状态表、事件日志、性能统计

**流水线时空分析（参考）：**




## 7. 实验感悟

> 以下为参考提纲，请结合自己的实际体验撰写（不少于 200 字）：

1. **对 Tomasulo 动态调度的理解**：通过实际运行模拟器，观察指令在保留站、Load/Store Buffer 中的流动和 CDB 广播过程，对寄存器重命名、乱序执行、精确异常等概念有了哪些更深入的认识？

2. **三种冲突在 Tomasulo 中的表现**：WAR 和 WAW 冲突为何被完全消除？RAW 冲突在动态调度下如何表现？与五段流水线中的冲突处理有何本质区别？

3. **CDB 广播延迟的影响**：执行完成到写结果再到依赖指令执行之间存在 2 个周期的延迟（推一周期 + 广播后等一周期），这对 CPI 有什么影响？如何通过硬件优化减少这一延迟？

4. **性能分析的体会**：分析不同场景下的 CPI 和停顿统计，长延迟指令（如 mul.d 的 10 周期）对流水线效率的影响。Tomasulo 动态调度在这种情况下相比顺序流水线有什么优势？

5. **模拟器实现的收获**：在实现 Tomasulo 模拟器的过程中，对保留站数据结构、寄存器状态表的 qi 标记、CDB 广播的传播机制、Mn 符号值跟踪等概念有了哪些理解上的提升？结合具体代码（如 `_issue_fp_arith()` 的寄存器重命名逻辑、`_broadcast()` 的全局传播、`_cdb_broadcast_rs()` 的 WAW 检查和计算表达式生成等），说明你对动态调度在代码层面的理解。

---

## 附录

### A. 模拟器运行说明

```bash
cd tomasulo/
python3 app.py
# 浏览器打开 http://localhost:5050
```

### B. 模拟器代码仓地址

